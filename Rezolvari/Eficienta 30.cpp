#include <iostream>
#include <fstream>
using namespace std;

ifstream fin("NUMERE.IN");

int n, x, y, a, b;

int main()
{
	fin >> n;

	fin >> x;

	a = x;

	while (fin >> y)
	{
		if ((int)y - int(x) == 0)
		{

		}
	}
}
